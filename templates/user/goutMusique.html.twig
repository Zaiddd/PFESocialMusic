{% extends 'base.html.twig' %}
{% block title %} Compléter mon profil - goûts musicaux {% endblock %}

{% block body %}
    <div class="mx-5 my-3">
        <h2> Préférences musicales </h2>
        <form action="{{ app.user.id }}/profil">
            <button type="submit" class="btn btn-secondary"> Retourner à mon profil </button>
        </form>
    </div>
    <div class="row mx-5 my-3 shadow-lg p-3 bg-white rounded">
        <h3 class="w-100"> Créer un compte </h3><br><br>
        {{ include('form/formGoutMusique.html.twig', {'button_label': 'Modifier'}) }}
    </div>
{% endblock %}